function setup() {
    createCanvas(300,300);
    background(210);
    

    //Regular PokeBall
    // // Sets color of outline
    // stroke("black");
    // // Makes the pixel thickness 6 px thick. Thickness of shape.
    // strokeWeight(6);

    // // makes an arc
    // //Width is the width of the canvas length is the length
    // //QUARTER_PI = constant of .25*pi
    // fill("white");
    // arc(width/2, length/2 + 110, 200, 200, 0, PI, CHORD);
    // //This comand is for the circle after it.
    // fill("red");
    
    // // The angles are the 5th and 6th parameters it starts at the fifth parameter and goes colckwise to the 6th parameter
    // arc(width/2, length/2 + 110, 200, 200, PI, 0, CHORD);
    // // I assume CHORD is a constant

    // ellipse(width/2, height/2 - 40, 20, 20,);
    // fill("white");



// Length isn't something. Only width and height are constants!
    ellipse(width/2, height/2, 60, 60);
    


    
}